import 'package:flutter/material.dart';

class ShareReceiveScreen extends StatelessWidget {
  const ShareReceiveScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Share Receive Screen'),
      ),
      body: Center(
        child: const Text('Share Receive Screen'),
      ),
    );
  }
}
