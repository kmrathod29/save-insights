import 'package:flutter/material.dart';

class SICard extends StatelessWidget {
  const SICard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card(
      child: Center(
        child: const Text('SI Card'),
      ),
    );
  }
}
